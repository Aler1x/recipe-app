import { Dimensions } from "react-native";

export const API_URL = 'https://ca24-37-73-3-150.ngrok-free.app'; // change this to server IP address (and make it work)
export const STEP_CIRCLE_SIZE = Dimensions.get('window').width * 0.1;
